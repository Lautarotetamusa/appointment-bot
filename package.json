{
    "name": "appointmentbot",
    "version": "1.0.0",
    "description": "",
    "license": "ISC",
    "author": "lautaro teta msua",
    "type": "commonjs",
    "main": "src/index.ts",
    "dependencies": {
        "dotenv": "^16.5.0",
        "drizzle-orm": "^0.44.2",
        "express": "^5.1.0",
        "pg": "^8.16.0",
        "zod": "^3.25.64"
    },
    "devDependencies": {
        "@types/dotenv": "^6.1.1",
        "@types/express": "^5.0.3",
        "drizzle-kit": "^0.31.1",
        "drizzle-seed": "^0.3.1",
        "nodemon": "^3.1.10",
        "ts-node": "^10.9.2",
        "typescript": "^5.8.3"
    },
    "scripts": {
        "build": "rm -rf dist/ && tsc",
        "start:dev": "nodemon src/server.ts",
        "start:prod": "rm -rf dist/ && tsc && node dist/src/server.js",
        "find-process": "ps -x | grep src/server.ts | grep -v grep",
        "kill-process": "ps -x | grep src/server.ts | grep -v grep | awk '{print $1}' | xargs kill -9"
    }
}
